/* Orden de lista inverso
En este kata crearás una función que toma una lista y devuelve una lista con el orden inverso.

Ejemplos (entrada -> salida)
* [1, 2, 3, 4]  -> [4, 3, 2, 1]
* [9, 2, 0, 7]  -> [7, 0, 2, 9] */

const reverseList = (list) => {
  return list.reverse();
};
